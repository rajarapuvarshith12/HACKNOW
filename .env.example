# Example environment variables
PORT=3000
FIREBASE_PROJECT_ID=quit-queue

# Path to service account JSON (keep this file off git)
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json

# or store the entire JSON as a string (be careful with quotes)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
